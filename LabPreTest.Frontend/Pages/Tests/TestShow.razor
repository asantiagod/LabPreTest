@page "/tests/show/{Id:int}"

@if (test is null)
{
    <Loading />
}
else
{
    <FormForShowTest @ref="testForm"
                 Label="Examen:"
                 Model="test"
                 OnValidSubmit="Return"
                 ReturnAction="Return" />
}
