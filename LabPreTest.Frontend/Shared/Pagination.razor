<div class="mb-2 form-floating">
    <select class="form-select" id="floatingInput" @bind="RecordsNumber" @bind:after="SelectCallback"> 
        <option value="2">2</option>
        <option value="5">5</option>
        <option value="10" selected="true">10</option>
        <option value="full">FULL</option>
    </select> 
    <label for="floatingInput">Records per page</label>
</div>

<nav>
    <ul class="pagination">
        @foreach (var link in links)
        {
            <li @onclick=@(() => InternalSelectedPage(link)) 
                style="cursor: pointer"
                class="page-item @(link.Enable ? null : "disabled") @(link.Enable ? "active" : null)">
                <a class="page-link">@link.Text</a>
            </li>
        }
    </ul>
</nav>
